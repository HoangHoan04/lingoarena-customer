import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Về LingoArena",
};

export default function AboutPage() {
  return <div className="flex flex-col gap-0 overflow-x-hidden">About</div>;
}
